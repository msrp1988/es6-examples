
// Perameterized Function for template strings

var printTemplateString = (studentId, studentName, studentMarks) => {

    // `content string ${dynamic value}`
    
    console.log("========== template strings ============")
    
    console.log(`Student Id : ${studentId},
                Student Name: ${studentName},
                Student Marks: ${studentMarks}`);
}